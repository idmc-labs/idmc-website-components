apiVersion: v2
name: idmc-website-components-helm
description: "Wrapper Helm chart for idmc-website-components, depends on web-app-serve-helm"
type: application
# Placeholder version; the CI (web-app-serve-action) rewrites SET-BY-CICD.
version: 0.0.1-SET-BY-CICD
dependencies:
  - name: web-app-serve-helm
    version: 0.2.1
    repository: oci://ghcr.io/toggle-corp
    alias: app
